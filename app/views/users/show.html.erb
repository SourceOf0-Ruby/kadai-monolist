
<div class="user-profile">
  <div class="icon text-center">
    <img class="image-circle" src="<%= gravator_url(@user, size: 100) %>" alt="<%= @user.name %> icon">
  </div>
  <div class="name text-center">
    <h1><%= @user.name %></h1>
  </div>
</div>
